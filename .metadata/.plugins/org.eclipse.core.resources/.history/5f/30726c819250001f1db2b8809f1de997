package com.aurionpro.behavioural.observer.model;

public class SMSNotifier implements IAccount{

	@Override
	public void notifyApp(Account account, int previousBalance) {
		
		System.out.println("SMS notification for " + account.notificationState + " " + account.getBalance());	
		
	}

}
