package com.aurionpro.behavioural.state.model;

import java.util.ListIterator;

public interface IPacket extends Iterator<String>{
	void next(Packet packet);
	void previous(Packet packet);
	void current(Packet packet);
	
	
	
	
}
