package com.aurionpro.behavioural.observer.model;

public class EmailNotifier implements IAccount{

	@Override
	public void notifyApp(Account account) {
		System.out.println("email notification - " + account.notificationState + " " + account.getBalance());
		
	}
	
}
